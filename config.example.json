{
  "_说明": "复制成 config.json 后填入真实值。config.json 不会进版本库。也可以改用环境变量 DEEPSEEK_API_KEY / ALIYUN_OCR_APPCODE，环境变量优先级更高。",

  "deepseek_api_key": "sk-在这里填你的-DeepSeek-key",
  "aliyun_ocr_appcode": "在这里填你的-阿里云-OCR-AppCode",

  "_ports说明": "各模块服务端口，可改；命令行 --port 优先级更高",
  "ports": {
    "writing": 8765,
    "speaking": 8766
  },

  "_python说明": "可选。给某模块指定 Python 解释器；留空/删掉则用当前解释器。口语需要装了 faster-whisper 的环境（如 conda 的 pytorch 环境）。",
  "python": {
    "speaking": "E:/conda_envs/pytorch/python.exe",
    "writing": null
  }
}
